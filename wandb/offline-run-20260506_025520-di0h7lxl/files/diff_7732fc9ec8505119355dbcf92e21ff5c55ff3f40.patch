diff --git a/configs/shared_schema.yaml b/configs/shared_schema.yaml
index 893a01e..48fdb35 100644
--- a/configs/shared_schema.yaml
+++ b/configs/shared_schema.yaml
@@ -25,6 +25,9 @@ env:
 training:
   seed: 42
   total_timesteps: 1000000
+  start_timesteps: 10000
+  eval_freq: 10000
+  n_eval_episodes: 100
   n_envs: 1
 search:
   max_paths: null
diff --git a/configs/td3_uni_masked_delta_qed.yaml b/configs/td3_uni_masked_delta_qed.yaml
index 7dd4ed4..81d8af8 100644
--- a/configs/td3_uni_masked_delta_qed.yaml
+++ b/configs/td3_uni_masked_delta_qed.yaml
@@ -19,6 +19,8 @@ training:
   seed: 42
   total_timesteps: 1000000
   start_timesteps: 10000
+  eval_freq: 10000
+  n_eval_episodes: 100
   n_envs: 1
 td3:
   actor_lr: 0.0001
diff --git a/genmolrl/algorithms/__pycache__/common.cpython-310.pyc b/genmolrl/algorithms/__pycache__/common.cpython-310.pyc
index 95035db..03ddfb0 100644
Binary files a/genmolrl/algorithms/__pycache__/common.cpython-310.pyc and b/genmolrl/algorithms/__pycache__/common.cpython-310.pyc differ
diff --git a/genmolrl/algorithms/a2c/__pycache__/train.cpython-310.pyc b/genmolrl/algorithms/a2c/__pycache__/train.cpython-310.pyc
index 1a6e819..5d770cb 100644
Binary files a/genmolrl/algorithms/a2c/__pycache__/train.cpython-310.pyc and b/genmolrl/algorithms/a2c/__pycache__/train.cpython-310.pyc differ
diff --git a/genmolrl/algorithms/a2c/train.py b/genmolrl/algorithms/a2c/train.py
index 6f0995e..e23c94d 100644
--- a/genmolrl/algorithms/a2c/train.py
+++ b/genmolrl/algorithms/a2c/train.py
@@ -16,7 +16,7 @@ def train(config: dict, experiment_name: str):
     config = dict(config)
     config["algorithm"] = "A2C"
     run = init_wandb(config, "A2C", experiment_name)
-    train_env, _eval_env = make_envs(config, seed)
+    train_env, eval_env = make_envs(config, seed)
 
     a2c_cfg = config["a2c"]
     policy = MaskedActorCriticPolicy if config["masking"] != "none" else "MlpPolicy"
@@ -41,7 +41,7 @@ def train(config: dict, experiment_name: str):
     )
     model.learn(
         total_timesteps=int(config["training"].get("total_timesteps", 1_000_000)),
-        callback=sb3_callbacks(config, run.id),
+        callback=sb3_callbacks(config, run.id, eval_env),
         progress_bar=bool(a2c_cfg.get("progress_bar", False)),
     )
     model.save(str(wandb.run.dir + "/final_model"))
diff --git a/genmolrl/algorithms/common.py b/genmolrl/algorithms/common.py
index 6b5b3f8..ea7a75a 100644
--- a/genmolrl/algorithms/common.py
+++ b/genmolrl/algorithms/common.py
@@ -14,7 +14,7 @@ from wandb.integration.sb3 import WandbCallback
 
 import wandb
 from genmolrl.config import project_root, resolve_path
-from genmolrl.logging.callbacks import EpisodeWandbCallback
+from genmolrl.logging.callbacks import EpisodeWandbCallback, EvaluationWandbCallback
 from genmolrl.logging.wandb_metrics import define_ppo_compatible_metrics
 from genmolrl.registry import ENV_ID, register_envs
 
@@ -108,24 +108,31 @@ def make_envs(config: dict, seed: int):
     return train_env, eval_env
 
 
-def sb3_callbacks(config: dict, run_id: str):
+def sb3_callbacks(config: dict, run_id: str, eval_env=None):
     paths = run_dir(run_id)
     ckpt_dir = paths / "checkpoints"
     ckpt_dir.mkdir(parents=True, exist_ok=True)
-    return CallbackList(
-        [
-            WandbCallback(
-                model_save_path=str(paths / "wandb_model"),
-                model_save_freq=int(config["callbacks"].get("model_save_freq", 5000)),
-                gradient_save_freq=int(config["callbacks"].get("gradient_save_freq", 100)),
-                log="all",
-                verbose=1,
-            ),
-            CheckpointCallback(
-                save_freq=int(config["callbacks"].get("model_save_freq", 5000)),
-                save_path=str(ckpt_dir),
-                name_prefix=config.get("algorithm", "model").lower(),
-            ),
-            EpisodeWandbCallback(),
-        ]
-    )
+    callbacks = [
+        WandbCallback(
+            model_save_path=str(paths / "wandb_model"),
+            model_save_freq=int(config["callbacks"].get("model_save_freq", 5000)),
+            gradient_save_freq=int(config["callbacks"].get("gradient_save_freq", 100)),
+            log="all",
+            verbose=1,
+        ),
+        CheckpointCallback(
+            save_freq=int(config["callbacks"].get("model_save_freq", 5000)),
+            save_path=str(ckpt_dir),
+            name_prefix=config.get("algorithm", "model").lower(),
+        ),
+        EpisodeWandbCallback(),
+    ]
+    if eval_env is not None:
+        callbacks.append(
+            EvaluationWandbCallback(
+                eval_env,
+                eval_freq=int(config["training"].get("eval_freq", 10000)),
+                n_eval_episodes=int(config["training"].get("n_eval_episodes", 100)),
+            )
+        )
+    return CallbackList(callbacks)
diff --git a/genmolrl/algorithms/ppo/__pycache__/train.cpython-310.pyc b/genmolrl/algorithms/ppo/__pycache__/train.cpython-310.pyc
index 5935c23..771b20e 100644
Binary files a/genmolrl/algorithms/ppo/__pycache__/train.cpython-310.pyc and b/genmolrl/algorithms/ppo/__pycache__/train.cpython-310.pyc differ
diff --git a/genmolrl/algorithms/ppo/train.py b/genmolrl/algorithms/ppo/train.py
index 5f36620..ee29f0d 100644
--- a/genmolrl/algorithms/ppo/train.py
+++ b/genmolrl/algorithms/ppo/train.py
@@ -15,7 +15,7 @@ def train(config: dict, experiment_name: str):
     config = dict(config)
     config["algorithm"] = "PPO"
     run = init_wandb(config, "PPO", experiment_name)
-    train_env, _eval_env = make_envs(config, seed)
+    train_env, eval_env = make_envs(config, seed)
 
     ppo_cfg = config["ppo"]
     cls = MaskablePPO if config["masking"] != "none" else PPO
@@ -38,7 +38,7 @@ def train(config: dict, experiment_name: str):
     )
     model.learn(
         total_timesteps=int(config["training"].get("total_timesteps", 1_000_000)),
-        callback=sb3_callbacks(config, run.id),
+        callback=sb3_callbacks(config, run.id, eval_env),
         progress_bar=bool(ppo_cfg.get("progress_bar", False)),
     )
     model.save(str(wandb.run.dir + "/final_model"))
diff --git a/genmolrl/algorithms/td3/__pycache__/train.cpython-310.pyc b/genmolrl/algorithms/td3/__pycache__/train.cpython-310.pyc
index af85e64..5945a75 100644
Binary files a/genmolrl/algorithms/td3/__pycache__/train.cpython-310.pyc and b/genmolrl/algorithms/td3/__pycache__/train.cpython-310.pyc differ
diff --git a/genmolrl/algorithms/td3/train.py b/genmolrl/algorithms/td3/train.py
index 35f1b2d..485b639 100644
--- a/genmolrl/algorithms/td3/train.py
+++ b/genmolrl/algorithms/td3/train.py
@@ -46,6 +46,62 @@ def _has_real_action(env, smiles: str | None) -> bool:
     return bool(env.unwrapped.reaction_manager.feasible_first_reactant_templates(smiles, kind=mask_kind))
 
 
+def _evaluate_td3(agent, eval_env, *, seed: int, eval_count: int, steps_done: int, n_eval_episodes: int) -> None:
+    previous_env = agent.env
+    agent.env = eval_env
+    episode_rewards: list[float] = []
+    episode_lengths: list[int] = []
+    episode_final_qeds: list[float] = []
+    episode_max_qeds: list[float] = []
+    try:
+        for episode_idx in range(1, n_eval_episodes + 1):
+            state, info = eval_env.reset(seed=seed + 1_000_000 + eval_count * 10_000 + episode_idx)
+            done = False
+            episode_reward = 0.0
+            episode_len = 0
+            max_qed = float(info.get("QED", 0.0))
+            final_qed = max_qed
+            while not done:
+                if not _has_real_action(eval_env, info.get("SMILES")) and not eval_env.unwrapped.use_stop_action:
+                    break
+                eval_env.enable()
+                action = agent.get_action(state, evaluate=True)
+                state, reward, terminated, truncated, info = eval_env.step(action)
+                done = bool(terminated or truncated)
+                episode_reward += float(reward)
+                episode_len += 1
+                final_qed = float(info.get("QED", 0.0))
+                max_qed = max(max_qed, final_qed)
+            episode_rewards.append(episode_reward)
+            episode_lengths.append(episode_len)
+            episode_final_qeds.append(final_qed)
+            episode_max_qeds.append(max_qed)
+            wandb.log(
+                {
+                    "train/global_step": steps_done,
+                    "eval/episode": episode_idx,
+                    "eval/total_reward_each_episode": episode_reward,
+                    "eval/source_train_global_step": steps_done,
+                },
+                step=steps_done,
+            )
+        wandb.log(
+            {
+                "train/global_step": steps_done,
+                "eval/mean_reward": float(np.mean(episode_rewards)) if episode_rewards else 0.0,
+                "eval/std_reward": float(np.std(episode_rewards)) if episode_rewards else 0.0,
+                "eval/mean_ep_length": float(np.mean(episode_lengths)) if episode_lengths else 0.0,
+                "eval/mean_final_qed": float(np.mean(episode_final_qeds)) if episode_final_qeds else 0.0,
+                "eval/max_qed": float(np.max(episode_max_qeds)) if episode_max_qeds else 0.0,
+                "eval/max_episode_qed": float(np.max(episode_max_qeds)) if episode_max_qeds else 0.0,
+                "eval_count": eval_count,
+            },
+            step=steps_done,
+        )
+    finally:
+        agent.env = previous_env
+
+
 def train(config: dict, experiment_name: str):
     seed = int(config["training"].get("seed", 42))
     set_seed(seed)
@@ -56,6 +112,7 @@ def train(config: dict, experiment_name: str):
     define_ppo_compatible_metrics()
 
     env = _make_td3_env(config, eval_env=False)
+    eval_env = _make_td3_env(config, eval_env=True)
     td3_cfg = config["td3"]
     train_cfg = config["training"]
     agent = TD3Agent(
@@ -84,6 +141,8 @@ def train(config: dict, experiment_name: str):
     start_timesteps = int(train_cfg.get("start_timesteps", 10000))
     batch_size = int(td3_cfg.get("batch_size", 64))
     save_freq = int(config["callbacks"].get("model_save_freq", 5000))
+    eval_freq = int(train_cfg.get("eval_freq", 10000))
+    n_eval_episodes = int(train_cfg.get("n_eval_episodes", 100))
     checkpoint_dir = project_root() / "runs" / run.id / "td3_checkpoints"
     checkpoint_dir.mkdir(parents=True, exist_ok=True)
 
@@ -92,6 +151,7 @@ def train(config: dict, experiment_name: str):
     completed_rewards: list[float] = []
     cumulative_reward = 0.0
     overall_max_qed = 0.0
+    eval_count = 0
     while steps_done < max_timesteps:
         episode_count += 1
         state, info = env.reset(seed=seed + episode_count)
@@ -145,6 +205,16 @@ def train(config: dict, experiment_name: str):
                 metrics = agent.train(replay_buffer, batch_size)
                 metrics.update({"train/global_step": steps_done, "steps_done": steps_done})
                 wandb.log(metrics, step=steps_done)
+            if eval_freq > 0 and steps_done >= start_timesteps and steps_done % eval_freq == 0:
+                eval_count += 1
+                _evaluate_td3(
+                    agent,
+                    eval_env,
+                    seed=seed,
+                    eval_count=eval_count,
+                    steps_done=steps_done,
+                    n_eval_episodes=n_eval_episodes,
+                )
             if steps_done % save_freq == 0:
                 agent.save_model(str(checkpoint_dir / f"checkpoint_{steps_done}.tar"), steps_done, episode_count, replay_buffer)
         completed_rewards.append(episode_reward)
diff --git a/genmolrl/logging/__pycache__/callbacks.cpython-310.pyc b/genmolrl/logging/__pycache__/callbacks.cpython-310.pyc
index 80fb14f..58466f9 100644
Binary files a/genmolrl/logging/__pycache__/callbacks.cpython-310.pyc and b/genmolrl/logging/__pycache__/callbacks.cpython-310.pyc differ
diff --git a/genmolrl/logging/__pycache__/wandb_metrics.cpython-310.pyc b/genmolrl/logging/__pycache__/wandb_metrics.cpython-310.pyc
index 2010d5d..ab4f63a 100644
Binary files a/genmolrl/logging/__pycache__/wandb_metrics.cpython-310.pyc and b/genmolrl/logging/__pycache__/wandb_metrics.cpython-310.pyc differ
diff --git a/genmolrl/logging/callbacks.py b/genmolrl/logging/callbacks.py
index b56cc22..843ba23 100644
--- a/genmolrl/logging/callbacks.py
+++ b/genmolrl/logging/callbacks.py
@@ -51,3 +51,84 @@ class EpisodeWandbCallback(BaseCallback):
                 step=step,
             )
         return True
+
+
+class EvaluationWandbCallback(BaseCallback):
+    """Evaluate the current SB3 policy and log PPO-compatible eval metrics."""
+
+    def __init__(self, eval_env, *, eval_freq: int = 10000, n_eval_episodes: int = 100):
+        super().__init__()
+        self.eval_env = eval_env
+        self.eval_freq = int(eval_freq)
+        self.n_eval_episodes = int(n_eval_episodes)
+        self.last_eval_step = 0
+        self.eval_count = 0
+
+    def _predict(self, obs):
+        try:
+            mask = self.eval_env.envs[0].unwrapped.action_masks()
+            action, _ = self.model.predict(obs, deterministic=True, action_masks=np.asarray([mask]))
+        except TypeError:
+            action, _ = self.model.predict(obs, deterministic=True)
+        return action
+
+    def _on_step(self) -> bool:
+        if self.eval_freq <= 0:
+            return True
+        if self.num_timesteps - self.last_eval_step < self.eval_freq:
+            return True
+        self.last_eval_step = int(self.num_timesteps)
+        self.eval_count += 1
+
+        episode_rewards = []
+        episode_lengths = []
+        episode_final_qeds = []
+        episode_max_qeds = []
+
+        for episode_idx in range(1, self.n_eval_episodes + 1):
+            obs = self.eval_env.reset()
+            done = False
+            episode_reward = 0.0
+            episode_length = 0
+            max_qed = 0.0
+            final_qed = 0.0
+            while not done:
+                action = self._predict(obs)
+                obs, rewards, dones, infos = self.eval_env.step(action)
+                reward = float(np.asarray(rewards).reshape(-1)[0])
+                info = infos[0] if infos else {}
+                done = bool(np.asarray(dones).reshape(-1)[0])
+                episode_reward += reward
+                episode_length += 1
+                q = float(info.get("QED", 0.0))
+                max_qed = max(max_qed, q)
+                final_qed = q
+
+            episode_rewards.append(episode_reward)
+            episode_lengths.append(episode_length)
+            episode_final_qeds.append(final_qed)
+            episode_max_qeds.append(max_qed)
+            wandb.log(
+                {
+                    "train/global_step": int(self.num_timesteps),
+                    "eval/episode": episode_idx,
+                    "eval/total_reward_each_episode": episode_reward,
+                    "eval/source_train_global_step": int(self.num_timesteps),
+                },
+                step=int(self.num_timesteps),
+            )
+
+        wandb.log(
+            {
+                "train/global_step": int(self.num_timesteps),
+                "eval/mean_reward": float(np.mean(episode_rewards)) if episode_rewards else 0.0,
+                "eval/std_reward": float(np.std(episode_rewards)) if episode_rewards else 0.0,
+                "eval/mean_ep_length": float(np.mean(episode_lengths)) if episode_lengths else 0.0,
+                "eval/mean_final_qed": float(np.mean(episode_final_qeds)) if episode_final_qeds else 0.0,
+                "eval/max_qed": float(np.max(episode_max_qeds)) if episode_max_qeds else 0.0,
+                "eval/max_episode_qed": float(np.max(episode_max_qeds)) if episode_max_qeds else 0.0,
+                "eval_count": self.eval_count,
+            },
+            step=int(self.num_timesteps),
+        )
+        return True
diff --git a/genmolrl/logging/wandb_metrics.py b/genmolrl/logging/wandb_metrics.py
index 820d626..3c0e956 100644
--- a/genmolrl/logging/wandb_metrics.py
+++ b/genmolrl/logging/wandb_metrics.py
@@ -14,6 +14,13 @@ PPO_COMPATIBLE_METRICS = [
     "eval/total_reward_each_episode",
     "eval/mean_reward",
     "eval/mean_ep_length",
+    "eval/max_qed",
+    "eval/max_episode_qed",
+    "eval/mean_final_qed",
+    "eval/std_reward",
+    "eval/source_train_global_step",
+    "eval/episode",
+    "eval_count",
     "avg_reward",
     "total_qed",
     "avg_qed",
diff --git a/runs/monitors/eval/0.monitor.csv b/runs/monitors/eval/0.monitor.csv
index 9862376..895e3ca 100644
--- a/runs/monitors/eval/0.monitor.csv
+++ b/runs/monitors/eval/0.monitor.csv
@@ -1,2 +1,2 @@
-#{"t_start": 1777999689.313765, "env_id": "GenMolRL-MoleculeDesign-v0"}
+#{"t_start": 1778032342.755905, "env_id": "GenMolRL-MoleculeDesign-v0"}
 r,l,t
diff --git a/runs/monitors/train/0.monitor.csv b/runs/monitors/train/0.monitor.csv
index e0f0906..cea9ad8 100644
--- a/runs/monitors/train/0.monitor.csv
+++ b/runs/monitors/train/0.monitor.csv
@@ -1,5 +1,2 @@
-#{"t_start": 1777999687.29931, "env_id": "GenMolRL-MoleculeDesign-v0"}
+#{"t_start": 1778032341.4367447, "env_id": "GenMolRL-MoleculeDesign-v0"}
 r,l,t
--1.243861,2,3.523848
--1.0,1,3.683586
-0.0,1,3.739844
