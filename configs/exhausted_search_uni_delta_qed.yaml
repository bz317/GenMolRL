project: MolSynthRL
experiment_name: ExhaustedSearch_Uni
reaction_mode: uni
masking: reaction_valid
reward: delta_qed
max_episode_len: 5
dataset:
  test_file: data/Uni/reactants_test.pkl
  templates_file: data/Uni/templates_unimolecolar_explicit.pkl
env:
  invalid_reaction_penalty: -1.0
  reward_round_digits: 3
training:
  seed: 42
search:
  max_paths:
  max_reactions:
  max_starts:
  max_r2_per_template:
  results_file: runs/exhausted_search_uni_results.txt
  overwrite_results: true
  use_wandb: true
